// SPDX-License-Identifier: MIT
pragma solidity ^0.8.30;

import "forge-std/Script.sol";
import "../src/RWA_Car.sol";

contract ListCarForSaleScript is Script {
// address rwaCarAddress = 0xYourDeployedRwaCarAddressHere;
// uint256 tokenId = 0; // ID of the minted car
// uint256 salePriceInUSD = 5000 * 1e8; // $5000 in 8 decimals
// function run() external {
//     vm.startBroadcast();
//     // RWA_Car car = RWA_Car(rwaCarAddress);
//     car.listCarForSale(tokenId, salePriceInUSD);
//     vm.stopBroadcast();
// }
}
